apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    appname: busybox_app1-1
  name: busybox-cephfs-pod-1
spec:
  selector:
    matchLabels:
      workload: busybox-cephfs
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        workload: busybox-cephfs
    spec:
      containers:
      - image: quay.io/ocsci/rdr-ocs-workload
        imagePullPolicy: IfNotPresent
        name: busybox
